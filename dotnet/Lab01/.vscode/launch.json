{
    "name": "Lab01 Docker Attach",
    "type": "coreclr",
    "request": "attach",
    "processId": "${command:pickProcess}",
    "pipeTransport": {
    "pipeProgram": "docker",
    "pipeArgs": ["exec", "-i", "debug-container"],
    "debuggerPath": "/vsdbg/vsdbg",
    "pipeCwd": "${workspaceFolder}",
    "quoteArgs": false,
    },
    "sourceFileMap":{
        "/app":"${workspaceFolder}"
    },
    "version": "0.2.0",
    "configurations": [
    
        
    ],
}